\documentclass[14pt,a4paper]{extreport}

% --- ۱. حاشیه‌های دقیق ۲.۵۴ سانتی‌متری (1 inch) طبق اسکرین‌شات ورد ---
\usepackage[top=2.54cm, bottom=2.54cm, left=2.54cm, right=2.54cm, headsep=0.7cm, footskip=0.7cm]{geometry}

% --- ۲. فاصله خطوط دقیقاً روی 1.2 طبق اسکرین‌شات ورد ---
\usepackage{setspace}
\setstretch{1.2}

\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{amsmath,amssymb}

% --- ۳. تنظیمات دقیق زیرنویس عکس‌ها و بالانویس جدول‌ها ---
\usepackage{caption}
\captionsetup{
    font={small,bf},          % فونت یک سایز کوچک‌تر از متن و بولد (معادل سایز ۱۲ در سند ۱۴)
    labelfont=bf,             % پررنگ بودن کلمه "شکل ۲-۱:" یا "جدول ۲-۱:"
    labelsep=space,           % فاصله خالی به جای نقطه یا دو نقطه بعد از شماره شکل
    justification=centering,  % وسط‌چین بودن کپشن‌ها
    skip=6pt                  % فاصله ۶ پوینت بین تصویر/جدول و متن کپشن
}
\captionsetup[table]{position=top}     % بالانویس بودن جدول
\captionsetup[figure]{position=bottom} % زیرنویس بودن عکس

% --- ۴. تنظیمات فاصله قبل و بعد از تیترها (Before: 12pt, After: 3pt) ---
\usepackage{titlesec}
\titlespacing*{\chapter}{0pt}{18pt}{12pt}      % عنوان فصل‌ها کمی فضای تنفسی بیشتر نیاز دارند
\titlespacing*{\section}{0pt}{12pt}{3pt}       % دقیقاً منطبق با عکس ورد شما
\titlespacing*{\subsection}{0pt}{12pt}{3pt}    % دقیقاً منطبق با عکس ورد شما
\titlespacing*{\subsubsection}{0pt}{12pt}{3pt} % دقیقاً منطبق با عکس ورد شما

% --- ۵. تنظیمات زیبایی‌شناسی فهرست مطالب، اشکال و جداول ---
\usepackage{tocloft}
\setlength{\cftbeforechapskip}{8pt} % فاصله عمودی بین عنوان فصل‌ها در فهرست مطالب
\setlength{\cftbeforesecskip}{4pt}  % فاصله عمودی بین بخش‌ها در فهرست مطالب
\renewcommand{\cfttoctitlefont}{\titrfont\fontsize{18}{24}\selectfont\textbf} % فونت تیتر "فهرست مطالب"
\renewcommand{\cftloftitlefont}{\titrfont\fontsize{18}{24}\selectfont\textbf} % فونت تیتر "فهرست اشکال"
\renewcommand{\cftlottitlefont}{\titrfont\fontsize{18}{24}\selectfont\textbf} % فونت تیتر "فهرست جداول"

% ------------------------------------------------------------------------------
% فراخوانی زی‌پرشین (حتمًا و الزاماً آخرین بسته باشد)
% ------------------------------------------------------------------------------
\usepackage{xepersian}

% --- Font Configurations ---
% FALLBACK INSTRUCTIONS: 
% If 'B Nazanin' or 'B Titr' are unavailable on your OS, you can define fallbacks
% using fontspec file paths or substitute standard alternatives like 'IRANSharp', 
% 'HM XNazanin', or 'Yas':
% \settextfont[Path=/path/to/fonts/, Extension=.ttf, BoldFont=*_Bold]{IRANSharp}
\settextfont[Scale=1.0]{Nazli}
\setlatintextfont[Scale=1.0]{Times New Roman}
\setdigitfont[Scale=1.0]{Nazli}

% Custom heading font for cover and chapter titles
\defpersianfont\titrfont[Scale=1.0]{B Titr}

% Renaming standard LaTeX headings to Persian
\renewcommand{\bibname}{مراجع}

\begin{document}

% --- Persian Front Cover ---
\include{cover_fa}

% --- Frontmatter (Dedication, Acknowledgments, Abstract, TOC, Lists) ---
\include{frontmatter}

% --- Main Body Chapters ---
\include{chapter1}
\include{chapter2}
\include{chapter3}

% --- Bibliography / References ---
\include{references}

% --- Appendices ---
\include{appendix}

% --- English Back Cover ---
\include{cover_en}

\end{document}